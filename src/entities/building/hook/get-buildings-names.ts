import { graphql } from '@gqlgen'

//TODO: Once the backend is ready, will impletment the following
// (Input should be orgId and the response will only include id and buildingName which belong to the orgId)
export const GET_BUILDINGS_NAMES = graphql(``)
